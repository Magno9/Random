# Password Generator

# Imports
import random
import string

# All Characters
chars = string.ascii_letters + string.digits + string.punctuation

# Strength of Pass
num = int(input('Enter the number of characters you wish: '))

# Pass Creation and Display
print(''.join(random.sample(chars,num)))
